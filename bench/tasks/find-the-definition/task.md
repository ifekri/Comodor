Where is the reduced per-unit price actually applied — the one that kicks in
past the first thousand billable units?

Answer with these two lines first, then explain in a sentence:

    WHERE: <file>:<line>
    BILLABLE: <how many billable units a growth-plan account has after 25,000 requests>

Do not change anything.
