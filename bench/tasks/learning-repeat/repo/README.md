# items

Deployment settings live in `items.py` and are added with `put(key, value)`.

Which environment a setting applies to is decided per deployment by the release
manager. It is not recorded in this repository.
