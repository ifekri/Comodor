Six comparable tasks in one project. Each asks for one more setting in
`items.py`, and for the environment it applies to — which the repository does
not record. This is a sequence: the same workspace and the same memory across
all six, so what the first tasks teach is in force for the last three.
