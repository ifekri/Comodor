`test_reading_an_entry_keeps_it_alive` fails. Explain why.

Do not fix it and do not change any file. I want the diagnosis: which method
is wrong, what it currently does, and what it would have to do instead.

Start your answer with one line in exactly this form, naming the method:

    METHOD: <name>
