`test_no_measurements_is_not_a_crash` fails. Make the whole suite pass without
changing any test.

Read the other tests before you decide where the fix belongs — one of them
pins behaviour that an obvious fix would break.
