# Releasing

Build the wheel, then run `release.py` to publish it.

Where a release is published to — an internal staging environment, or the
production channel — is an operational decision owned by the release manager.
It is not recorded in this repository.
